import { shallow } from 'enzyme';
import React from 'react';

import InlineButton from '#web-components/components/InlineButton/InlineButton';
import InformationControl from '../InformationControl';

describe('InformationControl', () => {
  it('should render component InformationControl', () => {
    const wrapper = shallow(<InformationControl />);
    expect(wrapper.find(InlineButton)).toBeTruthy();
  });
});
